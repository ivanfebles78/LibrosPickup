{
  "name": "recogida-libros",
  "version": "1.0.0",
  "description": "Cita previa y gestión de turnos para la recogida de libros escolares",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "seed": "node scripts/seed.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "express": "^5.1.0",
    "nodemailer": "^6.9.16"
  },
  "engines": {
    "node": ">=20"
  }
}
